# 工位页面设备连接问题解决总结

## 问题描述
用户访问 http://localhost:3000/client/workstation 页面，在服务模式中点击设备测试时报错：
```
Device simulation connection failed: PLC_M1 - 模拟设备 cmfeykv960002tmu854dygs7u 未找到
```

## 问题根源分析

### 1. 端口配置不一致
- **前端设备通信客户端**：配置为连接 `localhost:5001`
- **前端API路由**：硬编码连接 `localhost:5001`
- **后端实际监听端口**：`localhost:5000`

### 2. 模拟模式问题
- 页面代码检测到设备IP为 `127.0.0.1` 时会启用模拟模式
- 模拟模式的设备ID列表是硬编码的，不包含实际的设备ID
- 实际设备ID：`cmfeykv960002tmu854dygs7u` 和 `cmfeykv960003tmu887hloima`
- 模拟模式中的设备ID：`cmfarf98z0005tme429z87fhs` 和 `cmfargmpe0009tme4c78fmxdh`

### 3. 后端API地址配置错误
- 后端配置的前端API地址：`localhost:3009`
- 前端实际运行端口：`localhost:3000`

## 解决方案

### 步骤1：修复端口配置
1. **修改设备通信客户端配置**
   - 文件：`simple-mes/src/lib/device-communication/client.ts`
   - 将端口从 `5001` 改为 `5000`

2. **修改API路由中的后端地址**
   - 文件：`src/app/api/device-communication/devices/[id]/connect/route.ts`
   - 将 `dotnetServiceUrl` 从 `http://localhost:5001` 改为 `http://localhost:5000`
   
   - 文件：`src/app/api/device-communication/devices/[id]/status/route.ts`
   - 将 `dotnetServiceUrl` 从 `http://localhost:5001` 改为 `http://localhost:5000`

3. **修改后端配置**
   - 文件：`DeviceCommunicationService/appsettings.json`
   - 将 `FrontendApi.BaseUrl` 从 `http://localhost:3009` 改为 `http://localhost:3000`

### 步骤2：禁用模拟模式
- 文件：`src/app/client/workstation/page.tsx`
- 注释掉模拟模式相关代码（第1129-1178行）
- 让所有设备都使用真实的后端设备通信服务

### 步骤3：修复设备配置同步
- 文件：`DeviceCommunicationService/Services/DeviceConfigSyncService.cs`
- 更新响应解析逻辑，正确处理前端API返回的JSON格式

## 验证结果

### 1. 后端服务状态
```powershell
# 检查健康状态
Invoke-WebRequest -Uri http://localhost:5000/api/health -Method Get

# 结果：
status: healthy
timestamp: 2025/9/12 4:55:41
version: 1.0.0
```

### 2. 设备配置同步
```powershell
# 刷新设备配置
Invoke-WebRequest -Uri "http://localhost:5000/api/configsync/refresh" -Method Post

# 查看同步状态
Invoke-WebRequest -Uri "http://localhost:5000/api/configsync/status" -Method Get

# 结果：
totalDevices: 1
connectedDevices: 0
devices: PLC_M1 (cmfeykv960003tmu887hloima)
```

### 3. 设备连接测试
```powershell
# 测试设备连接
$deviceId = "cmfeykv960003tmu887hloima"
Invoke-WebRequest -Uri "http://localhost:3000/api/device-communication/devices/$deviceId/connect" -Method Post

# 结果：
success: True
message: Device connected successfully
status: CONNECTED
```

## 使用说明

### 1. 启动服务
```powershell
# 启动后端服务
cd D:\Program\SimpleMES\DeviceCommunicationService\DeviceCommunicationService
dotnet run

# 启动前端服务
cd D:\Program\SimpleMES\simple-mes
npm run dev
```

### 2. 访问页面
1. 打开浏览器访问：http://localhost:3000/client/login
2. 登录后进入工位页面
3. 点击菜单中的"服务模式"
4. 点击设备的"测试"按钮

### 3. 设备测试流程
1. 系统首先检查设备是否已连接
2. 如果未连接，调用后端API建立连接
3. 连接成功后显示设备测试界面
4. PLC设备显示专用的PLC测试界面
5. 其他设备显示通用测试界面

## 关键代码位置

### 前端
- **设备连接测试函数**：`src/app/client/workstation/page.tsx` (第1090-1255行)
- **设备通信客户端**：`src/lib/device-communication/client.ts`
- **设备连接API**：`src/app/api/device-communication/devices/[id]/connect/route.ts`
- **设备状态API**：`src/app/api/device-communication/devices/[id]/status/route.ts`

### 后端
- **设备管理器**：`DeviceCommunicationService/Services/DeviceManager.cs`
- **设备执行控制器**：`DeviceCommunicationService/Controllers/DeviceExecutionController.cs`
- **配置同步服务**：`DeviceCommunicationService/Services/DeviceConfigSyncService.cs`
- **PLC驱动**：`DeviceCommunicationService/Drivers/PlcDriver.cs`

## 注意事项

1. **PLC模拟器**：后端在开发模式下会启动PLC模拟器，监听端口5020
2. **设备ID格式**：
   - 数据库ID（id）：`cmfeykv960002tmu854dygs7u`
   - 实例ID（instanceId/deviceId）：`cmfeykv960003tmu887hloima`
   - API调用时使用instanceId
3. **设备类型映射**：
   - 前端：`PLC_CONTROLLER`
   - 后端：`PLC`
   - API会自动进行转换
4. **连接超时**：设置为5秒，可在需要时调整

## 后续优化建议

1. **使用环境变量**：避免硬编码服务地址和端口
2. **统一配置管理**：创建配置文件集中管理所有服务地址
3. **改进错误处理**：提供更详细的错误信息和恢复建议
4. **添加重连机制**：设备断开后自动尝试重连
5. **优化模拟模式**：动态获取设备列表而不是硬编码