# MES系统设备通信问题解决方案

## 问题诊断

发现的主要问题：
1. **端口配置不匹配**：前端设备通信客户端配置的后端地址是 `localhost:5001`，但后端实际监听 `localhost:5000`
2. **前端API地址错误**：后端配置的前端API地址是 `localhost:3009`，但前端实际运行在 `localhost:3000`

## 已执行的修复

### 1. 修复前端配置（已完成）
- 文件：`simple-mes/src/lib/device-communication/client.ts`
- 修改：将 `baseUrl` 从 `http://localhost:5001` 改为 `http://localhost:5000`
- 修改：将 `websocketUrl` 从 `ws://localhost:5001` 改为 `ws://localhost:5000`

### 2. 修复后端配置（已完成）
- 文件：`DeviceCommunicationService/DeviceCommunicationService/appsettings.json`
- 修改：将 `FrontendApi.BaseUrl` 从 `http://localhost:3009` 改为 `http://localhost:3000`

### 3. 修复设备配置同步服务（已完成）
- 文件：`DeviceCommunicationService/DeviceCommunicationService/Services/DeviceConfigSyncService.cs`
- 修改：更新了响应解析逻辑，支持前端API返回的 `{ success: true, workstations: [...] }` 格式

## 运行服务

### 启动后端服务
```powershell
cd D:\Program\SimpleMES\DeviceCommunicationService\DeviceCommunicationService
dotnet run
```
服务将在 http://localhost:5000 上运行

### 启动前端服务
```powershell
cd D:\Program\SimpleMES\simple-mes
npm run dev
```
服务将在 http://localhost:3000 上运行

## 测试设备通信

### 1. 检查后端健康状态
```powershell
Invoke-WebRequest -Uri http://localhost:5000/api/health -Method Get
```

### 2. 刷新设备配置
```powershell
Invoke-WebRequest -Uri http://localhost:5000/api/configsync/refresh -Method Post
```

### 3. 查看同步状态
```powershell
Invoke-WebRequest -Uri http://localhost:5000/api/configsync/status -Method Get
```

### 4. 访问前端页面
- 设备通信测试页面：http://localhost:3000/admin/device-communication
- 设备通信演示页面：http://localhost:3000/device-communication-demo
- 工位测试页面：http://localhost:3000/workstation-test

## 验证结果

当前系统状态：
- ✅ 后端服务正常运行在端口 5000
- ✅ 前端服务正常运行在端口 3000
- ✅ 设备配置同步成功（已同步1个PLC设备）
- ✅ 前后端通信正常

## 后续建议

1. **添加环境变量配置**：建议使用环境变量来配置服务端口和API地址，避免硬编码
2. **添加自动重连机制**：在设备通信客户端添加自动重连逻辑
3. **改进错误处理**：添加更详细的错误日志和用户友好的错误提示
4. **添加监控仪表板**：创建一个实时监控页面显示所有设备的连接状态

## 常见问题

### Q: 设备无法连接怎么办？
A: 
1. 检查设备IP地址和端口是否正确
2. 确认设备在网络中可达（使用ping测试）
3. 检查防火墙设置
4. 查看后端日志获取详细错误信息

### Q: 设备配置没有同步怎么办？
A: 
1. 手动调用刷新接口：`POST http://localhost:5000/api/configsync/refresh`
2. 检查前端数据库中是否有设备数据
3. 查看后端日志确认同步过程是否有错误

### Q: WebSocket连接失败怎么办？
A: 
1. 确认后端服务正在运行
2. 检查WebSocket端口是否被占用
3. 查看浏览器控制台的错误信息